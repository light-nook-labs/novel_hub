name: sfacg-metadata-crawler-b

on:
  schedule:
    - cron: "14 01 * * *"
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      GH_TOKEN: ${{ github.token }}

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Setup uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        run: uv sync

      - name: Read BEGIN
        run: |
          if [ -f BEGIN.txt ]; then
            echo "BEGIN=$(cat BEGIN.txt)" >> "$GITHUB_ENV"
          else
            echo "BEGIN=1" >> "$GITHUB_ENV"
          fi

      - name: Set NUM
        run: |
          echo "NUM=200" >> "$GITHUB_ENV"

      - name: Run spider
        run: |
          set -o pipefail
          mkdir -p output
          uv run scrapy crawl meta_spider -a begin=${{ env.BEGIN }} -a num=${{ env.NUM }} -o output/meta_b.jsonl

      - name: Update BEGIN
        run: |
          NEW_BEGIN=$(( ${{ env.BEGIN }} + ${{ env.NUM }} - 5 ))
          if (( NEW_BEGIN < 1 )); then
            NEW_BEGIN=1
          fi
          echo $NEW_BEGIN > BEGIN.txt

      - name: Commit and push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions@github.com"
          git add output/meta_b.jsonl
          git add -f BEGIN.txt
          git commit -m "Auto update" --allow-empty
          git push origin main