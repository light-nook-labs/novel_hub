[project]
name = "sfacg-metadata-crawler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.2",
    "pytest>=9.0.3",
    "python-dotenv>=1.2.2",
    "scrapy>=2.15.2",
    "sqlmodel>=0.0.38",
]

[tool.pytest]
testpaths = ["meta_spider/tests"]
pythonpath = ["."]
